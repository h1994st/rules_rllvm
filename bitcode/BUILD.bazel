package(default_visibility = ["//visibility:public"])

exports_files([
    "providers.bzl",
    "compile.bzl",
    "aspect.bzl",
    "defs.bzl",
    "toolchain.bzl",
])

toolchain_type(name = "toolchain_type")
